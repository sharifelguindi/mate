#!/bin/sh

set -o errexit
set -o nounset

# Remove any existing node_modules from the host
rm -rf node_modules package-lock.json

# Install dependencies fresh in the container
echo "Installing npm dependencies..."
npm install

echo "Starting Vite dev server..."
exec npm run dev